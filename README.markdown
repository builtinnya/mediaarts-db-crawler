# mediaarts-db-crawler

A web crawler for [Media Arts Database](http://mediaarts-db.jp/).

## Usage

Install dependencies:

```shell
npm install
```

To start an anime DB crawler:

```shell
make anime
```
